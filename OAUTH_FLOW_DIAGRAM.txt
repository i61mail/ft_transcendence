```
╔═══════════════════════════════════════════════════════════════════════════╗
║                    GOOGLE OAUTH 2.0 FLOW DIAGRAM                         ║
╚═══════════════════════════════════════════════════════════════════════════╝

┌──────────────┐
│   Browser    │
│  (Frontend)  │
└───────┬──────┘
        │
        │ [1] User clicks "Sign in with Google"
        │
        ↓
   ┌────────────────────────────────────────────┐
   │ window.location.href =                     │
   │ "http://localhost:4000/auth/google"        │
   └────────────────┬───────────────────────────┘
                    │
                    │ [2] GET /auth/google
                    ↓
        ┌───────────────────────┐
        │   Fastify Backend     │
        │  @fastify/oauth2      │
        └───────┬───────────────┘
                │
                │ [3] 302 Redirect to Google
                │
                ↓
   ┌─────────────────────────────────────────────┐
   │  https://accounts.google.com/o/oauth2/...   │
   │  client_id=xxx                              │
   │  redirect_uri=.../callback                  │
   │  scope=profile email                        │
   └───────────────┬─────────────────────────────┘
                   │
                   │ [4] User authorizes
                   ↓
        ┌──────────────────────┐
        │   Google Auth        │
        │   Consent Screen     │
        └──────────┬───────────┘
                   │
                   │ [5] 302 Redirect with auth code
                   │
                   ↓
   ┌─────────────────────────────────────────────┐
   │ http://localhost:4000/auth/google/callback  │
   │ ?code=4/xxxxx...                            │
   └────────────────┬────────────────────────────┘
                    │
                    │ [6] GET /auth/google/callback
                    ↓
        ┌───────────────────────┐
        │   Fastify Backend     │
        │   oauth.ts handler    │
        └───────┬───────────────┘
                │
                │ [7] Exchange code for token
                ↓
   ┌─────────────────────────────────────────────┐
   │ POST https://oauth2.googleapis.com/token    │
   │ code=xxx&client_id=xxx&client_secret=xxx    │
   └────────────────┬────────────────────────────┘
                    │
                    │ [8] Returns access_token
                    ↓
        ┌───────────────────────┐
        │  Fastify Backend      │
        └───────┬───────────────┘
                │
                │ [9] Fetch user info
                ↓
   ┌─────────────────────────────────────────────┐
   │ GET https://www.googleapis.com/oauth2/v2/   │
   │     userinfo                                │
   │ Authorization: Bearer <access_token>        │
   └────────────────┬────────────────────────────┘
                    │
                    │ [10] Returns user data
                    │     { id, email, name, picture }
                    ↓
        ┌───────────────────────┐
        │  Fastify Backend      │
        │  Database Operations  │
        └───────┬───────────────┘
                │
                │ [11] Check if user exists
                │      by google_id or email
                ↓
        ╔═══════════════════╗
        ║  User exists?     ║
        ╚═══════╤═══════════╝
                │
      ┌─────────┴─────────┐
      │                   │
     YES                 NO
      │                   │
      ↓                   ↓
  ┌─────────┐      ┌──────────────┐
  │  Login  │      │ Create User  │
  │  User   │      │ in Database  │
  └────┬────┘      └──────┬───────┘
       │                  │
       └────────┬─────────┘
                │
                │ [12] Generate JWT token
                ↓
        ┌───────────────────────┐
        │  jwt.sign()           │
        │  { id, email }        │
        └───────┬───────────────┘
                │
                │ [13] Set HTTP-only cookie
                ↓
        ┌───────────────────────────────┐
        │ reply.setCookie(               │
        │   'access_token',              │
        │   token,                       │
        │   {                            │
        │     httpOnly: true,            │
        │     secure: prod,              │
        │     sameSite: 'lax',           │
        │     maxAge: 7 days             │
        │   }                            │
        │ )                              │
        └───────────┬───────────────────┘
                    │
                    │ [14] 302 Redirect to frontend
                    ↓
   ┌─────────────────────────────────────────────┐
   │ http://localhost:3000/dashboard             │
   │ Cookie: access_token=jwt_token_here         │
   └────────────────┬────────────────────────────┘
                    │
                    │ [15] Page loads
                    ↓
        ┌───────────────────────┐
        │   Dashboard Page      │
        │   useEffect() runs    │
        └───────┬───────────────┘
                │
                │ [16] Verify auth
                ↓
   ┌─────────────────────────────────────────────┐
   │ fetch('/auth/me', {                         │
   │   credentials: 'include'                    │
   │ })                                          │
   └────────────────┬────────────────────────────┘
                    │
                    │ [17] Cookie sent automatically
                    ↓
        ┌───────────────────────┐
        │  Fastify Backend      │
        │  Verify JWT           │
        └───────┬───────────────┘
                │
                │ [18] Return user data
                ↓
        ┌───────────────────────┐
        │   Dashboard Page      │
        │   Shows user info     │
        └───────────────────────┘
                │
                ✓
        ┌───────────────────────┐
        │   USER LOGGED IN! 🎉  │
        └───────────────────────┘


╔═══════════════════════════════════════════════════════════════════════════╗
║                         KEY SECURITY POINTS                               ║
╚═══════════════════════════════════════════════════════════════════════════╝

🔒 Client Secret: NEVER sent to frontend (stays in backend .env)
🔒 Access Token: Never exposed to JavaScript (HTTP-only cookie)
🔒 Authorization Code: Single-use, exchanged server-side
🔒 User Info: Fetched server-side with access token
🔒 JWT Token: Stored in HTTP-only cookie (XSS protection)
🔒 Cookie: SameSite=lax (CSRF protection)
🔒 HTTPS: Required in production (secure flag)


╔═══════════════════════════════════════════════════════════════════════════╗
║                            DATA FLOW                                      ║
╚═══════════════════════════════════════════════════════════════════════════╝

Frontend → Backend:    Browser redirect (no sensitive data)
Backend → Google:      Authorization code exchange (server-to-server)
Google → Backend:      Access token + user info (server-to-server)
Backend → Database:    User creation/lookup
Backend → Frontend:    Cookie set in redirect response
Frontend → Backend:    Cookie sent automatically on all requests


╔═══════════════════════════════════════════════════════════════════════════╗
║                        COOKIE DETAILS                                     ║
╚═══════════════════════════════════════════════════════════════════════════╝

Name:        access_token
Value:       eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9... (JWT)
Domain:      localhost (auto-set)
Path:        /
HttpOnly:    ✓ (JavaScript cannot read)
Secure:      ✓ (production only, requires HTTPS)
SameSite:    Lax (CSRF protection)
Max-Age:     604800 seconds (7 days)
Expires:     (calculated from Max-Age)


╔═══════════════════════════════════════════════════════════════════════════╗
║                    ENVIRONMENT VARIABLES                                  ║
╚═══════════════════════════════════════════════════════════════════════════╝

Backend (.env):
├── GOOGLE_CLIENT_ID=xxxxx.apps.googleusercontent.com
├── GOOGLE_CLIENT_SECRET=xxxxx
├── GOOGLE_CALLBACK_URL=http://localhost:4000/auth/google/callback
├── JWT_SECRET=random-secret-key
├── FRONTEND_ORIGIN=http://localhost:3000
├── NODE_ENV=development
└── PORT=4000

Google Console:
├── OAuth Client ID: xxxxx.apps.googleusercontent.com
├── OAuth Client Secret: xxxxx
├── Authorized redirect URIs:
│   └── http://localhost:4000/auth/google/callback
└── Authorized JavaScript origins:
    ├── http://localhost:3000
    └── http://localhost:4000


╔═══════════════════════════════════════════════════════════════════════════╗
║                      ERROR HANDLING                                       ║
╚═══════════════════════════════════════════════════════════════════════════╝

Error during OAuth:
    Backend catches error
         ↓
    Logs error details
         ↓
    Redirects to: http://localhost:3000?error=oauth_failed
         ↓
    Frontend shows error message


╔═══════════════════════════════════════════════════════════════════════════╗
║                    TESTING ENDPOINTS                                      ║
╚═══════════════════════════════════════════════════════════════════════════╝

✓ http://localhost:4000/auth/google
  → Should redirect to Google

✓ http://localhost:4000/auth/google/callback?code=xxx
  → Should create user and redirect to dashboard

✓ http://localhost:4000/auth/me
  → Should return user info (with cookie)

✓ http://localhost:3000
  → Should show login page with Google button

✓ http://localhost:3000/dashboard
  → Should verify auth and show user info


╔═══════════════════════════════════════════════════════════════════════════╗
║                     DATABASE SCHEMA                                       ║
╚═══════════════════════════════════════════════════════════════════════════╝

Table: users
┌────────────────┬──────────────┬──────────┬─────────────────────────────┐
│ Column         │ Type         │ Required │ Notes                       │
├────────────────┼──────────────┼──────────┼─────────────────────────────┤
│ id             │ INTEGER      │ YES      │ Primary key, auto-increment │
│ email          │ TEXT         │ YES      │ Unique                      │
│ username       │ TEXT         │ YES      │ Unique                      │
│ password       │ TEXT         │ NO       │ NULL for OAuth users        │
│ avatar_url     │ TEXT         │ NO       │ Profile picture URL         │
│ google_id      │ TEXT         │ NO       │ Google user ID (unique)     │
│ auth_provider  │ TEXT         │ NO       │ 'local' or 'google'         │
│ created_at     │ DATETIME     │ NO       │ Auto-set to current time    │
└────────────────┴──────────────┴──────────┴─────────────────────────────┘

Example OAuth user:
{
  id: 42,
  email: "user@gmail.com",
  username: "john_doe_1234567890",
  password: null,
  avatar_url: "https://lh3.googleusercontent.com/...",
  google_id: "1234567890",
  auth_provider: "google",
  created_at: "2025-11-27 10:30:00"
}


╔═══════════════════════════════════════════════════════════════════════════╗
║                        SUCCESS! 🎉                                        ║
╚═══════════════════════════════════════════════════════════════════════════╝

You now have a complete, secure OAuth 2.0 implementation!

Next steps:
1. Get Google OAuth credentials
2. Add them to .env file
3. npm run dev (backend)
4. npm run dev (frontend)
5. Click "Sign in with Google"
6. Celebrate! 🎊
```
